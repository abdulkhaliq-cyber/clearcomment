{
  "name": "clearcomment-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "cd client && npm run dev",
    "worker": "cd railway-worker && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../railway-worker && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}